import SyncHeader from "../components/SyncHeader";
import { Wrapper } from "../styles/Wrapper";

/**
 * This is the second page with steps,
 * TODO: Remove the dummy content and add actual ones later
 */
const ConfigSteps = () => {
	return (
		<Wrapper>
			<SyncHeader />
			TODO: Config steps
		</Wrapper>
	);
};

export default ConfigSteps;
