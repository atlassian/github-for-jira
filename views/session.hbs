<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="ap-local-base-url" content="{{localBaseUrl}}">
  <meta name="public-url" content="{{APP_URL}}">
  <title>{{title}}</title>
</head>
<body>
Logging you into Github, one moment...
<script nonce="{{nonce}}">
  setTimeout(function() {
    const jiraHost = window.localStorage.getItem("jiraHost");
    if (jiraHost) {
      // Set jiraHost value
      document.cookie = "jiraHost=" + jiraHost+"; path=/; expires=" + new Date(Date.now() + (60000 * 5)).toUTCString();
      // Remove old value
      window.localStorage.removeItem("jiraHost");
    }
    window.location.href = "{{redirectUrl}}";
  }, 30000);
</script>
</body>
</html>
