describe("jira-connected-repos-get", () => {

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should return 401 when no JWT was provided", () => {

	});

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should return 400 when no subscription was found", () => {

	});

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should return 400 if the subscription belongs to a different user", () => {

	});

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should return 400 when unknown filtered status was provided", () => {

	});

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should return 400 when a page size is too great", () => {

	});

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should return the first page of repos by default without any filters", ()=> {

	});

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should correctly apply repoSearch filter", () => {

	});

	// eslint-disable-next-line @typescript-eslint/no-empty-function
	it("should correctly apply pagination", () => {

	});
});
