import { Request } from "express";
import GitHubClient from "./client/github-client";

export default async (): Promise<void> => {
	console.log("Pushing...");
	// const github = new GitHubClient(1, context.log);
};
