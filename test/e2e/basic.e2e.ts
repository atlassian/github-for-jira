import { test } from "@playwright/test";
// import { GithubClient } from "test/e2e/utils/github-client";
// import { deleteDevInfoDataByInstallationId } from "test/e2e/utils/helpers";

/*const githubClient = new GithubClient({
	oauthToken: process.env.GITHUB_OAUTH_TOKEN as string
});*/

// const deleteGitHubRepo = () => githubClient.deleteRepository(ORGANIZATION, REPO_NAME);

test.describe("Basic", () => {
	/*test("install", async ({ page }) => {

	});*/
});

